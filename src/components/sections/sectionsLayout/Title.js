import React from 'react';

const Title = ({ title }) => {
  return (
    <header className='text-center text-primary section-title'>
      <h2>{title}</h2>
    </header>
  );
};

export default Title;
